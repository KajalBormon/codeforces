#include <iostream>
#include <algorithm>
#include <math.h>
using namespace std; 

int main() {
    string s, t; 
    cin >> s;
    cin >> t;

    cout << s.size() << " " << t.size() << endl;
    cout << s << " " << t << endl; 
    return 0; 
}